DATABASE_URL=postgresql://user:password@localhost:5432/preevol
SESSION_EXPIRY_DAYS=7

# After running migrations, manually approve your first admin:
# UPDATE users SET account_status = 'approved', is_admin = true, admin_requested = false WHERE email = 'your-admin@example.com';
# INSERT INTO roles (role_name, company) VALUES ('Admin', NULL), ('User', NULL);
